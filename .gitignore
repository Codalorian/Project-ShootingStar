# --- secrets: never commit ---
token.txt
*.token
.env
.env.*
*.pem
credentials.json

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.ipynb_checkpoints/

# --- build artifacts ---
bench/roofline
bench/ssd_roofline
bench/*.o

# --- benchmark scratch: the storage roofline test file is 32 GiB ---
*.dat
ssd_roofline.dat

# --- results ---
# The atlas/roofline/ceiling records ARE the evidence behind docs/, and they are
# small (~500K total), so they are tracked. Only run logs and scratch stay out.
results/*.log

# --- data / models ---
data/*
!data/.gitkeep
!data/README.md
*.safetensors
*.gguf
